<main>
    <section class="section">
        <div class="container my-container">
            <div class="row">
                {% for item in MODULOS %}
                    <div class="col-xl-3 col-lg-3 col-md-4 col-6 pr-2 pl-2 pb-2 "
                         onclick="window.location.href = '/{{ item.CONTROLLER}}/'">
                        <div class="card card-menu nao-seleciona-texto cursor-pointer mb-2 ">
                            <div class="card-body flex-column justify-content-center align-items-center text-center pt-3">
                                <div class="card-menu-icone">
                                    <i class="mdi mdi-36px {{ item.ICONE}}"></i>
                                </div>
                                <div id="div{{ item.CONTROLLER}}">
                                    <h6 class="card-menu-titulo mb-0">{{ item.TITULO}}</h6>
                                    <p class="card-menu-subtitulo pt-1">{{ item.DESCRICAO}}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="col">
                        <div class="alert alert-light">
                            <div class="alert-title">Atenção</div>
                            Você não tem privilégios de acessar os serviços, entre em contato com o administrador e solicitar liberação
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
</main>