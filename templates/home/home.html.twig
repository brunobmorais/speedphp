<main>
    <section class="section">

        <div class="container my-container">
            <div class="row">
                {% for item in MODULOS %}
                    <div class="col-xl-3 col-lg-4 col-md-4 col-6 pr-1 pl-1 pb-1"
                         onclick="window.location.href = '/{{ item.CONTROLLER}}/'" style="padding-right: 7px; padding-left: 7px">
                        <div class="card card-menu nao-seleciona-texto cursor-pointer mb-2 ">
                            <div class="card-body card-menu-body flex-column justify-content-center align-items-start text-start">
                                <div class="card-menu-icone pb-2 pb-md-2">
                                    <div class="icon-circle">
                                        <i class="mdi mdi-36 {{ item.ICONE}}"></i>
                                    </div>
                                </div>
                                <div id="div{{ item.CONTROLLER}}">
                                    <h6 class="card-menu-titulo mb-0">{{ item.TITULO}}</h6>
                                    <p class="card-menu-subtitulo pt-1 d-none d-sm-block">{{ item.DESCRICAO}}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="col">
                        <div class="alert alert-light">
                            <div class="alert-title">Atenção</div>
                            Você não tem privilégios de acessar os serviços, entre em contato com o administrador e solicitar liberação
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
</main>