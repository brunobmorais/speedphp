<x-main.container>
    <x-div.card>
        <form id="formUsuarioCadastro" class="needs-validation" novalidate
              action="{{ SERVICO.URL }}-cadastro-action" method="post"
              enctype='multipart/form-data' onsubmit="return checkCPF();">
            <input hidden name='CODPESSOA' value='{{ PESSOA.CODPESSOA }}'>

            <div class="row">
                <div class="section-title col-12">Informações</div>
                {% embed "gestaopessoas/components/div_pessoa.twig" %}
                    {% block divInfoAdicional %}
                        {% embed  "gestaopessoas/components/div_pessoafisica.twig" %}{% endembed %}
                    {% endblock %}
                {% endembed %}
                {% embed "gestaopessoas/components/div_endereco.twig" %}{% endembed %}
            </div>

            <div class="row py-3">
                <div class="col-12">
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end col-12">

                        <button type="submit" id="btnSalvarUsuario"
                                class="btn btn-lg btn-primary btn-round col-md-3 col-lg-2 float-right mt-2">
                            Salvar
                        </button>
                    </div>
                </div>
            </div>
        </form>
    </x-div.card>
</x-main.container>
